CREATE TABLE student(
	student_id INT, --ova ke bide primaren kluc so tip integer
  	name VARCHAR(20), --ime na studentot so data type varchar so length 20
  	nasoka VARCHAR(20),
  	PRIMARY KEY(student_id) --vikame deka primaren kluc ke bide student id
  	
);
